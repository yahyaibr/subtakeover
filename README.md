# subtakeover
Python CLI tool: it resolves DNS (CNAME/A) for a list of subdomains, then matches the response against the well-known fingerprint database used by can-i-take-over-xyz to flag likely-vulnerable services.
